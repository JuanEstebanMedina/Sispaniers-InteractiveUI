# Gmail SMTP (App Password, not OAuth)

# HTTP server
PORT=8000

# No persistence yet — receiving/sending an email is only logged (warn level),
# nothing is stored or looked up. Storage (in-memory, then Mongo) is a later phase.
